# BizMind — 待办清单（2026-06-16）

> **已 supersede：** 请使用最新待办 [todo0619.md](./todo0619.md)。本文保留作 v0.4 收尾审计记录。  
> 完成度快照与后续任务。文档已精简为 `design` / `api` / `dev` / 本文。  
> **最后审计：** 2026-06-16（本地 pytest 38 passed，覆盖率 55%）

---

## 1. 完成度总览

| 维度 | 评估 |
|------|------|
| **整体 MVP** | ~**85%** |
| **面试就绪** | ~**88%**（可 Demo，有 benchmark 初步数据 + 演练脚本） |

| 模块 | 状态 | 说明 |
|------|------|------|
| P1 Baseline RAG | ✅ ~95% | Auth、文档索引（Markdown）、Baseline SSE、Docker |
| P2 Agent | ✅ ~95% | LangGraph 全链路、Hybrid、Parent-Child、Agent SSE |
| P3 生产特性 | 🚧 ~80% | 多租户、stale、限流、Embedding 缓存、跨租户测试；**PDF 解析未实现** |
| P4 评测包装 | 🚧 ~55% | Eval API + Eval 页 + `eval_rag.py` CLI 已有；**真实 RAGAS 跑分、README benchmark、Langfuse 缺** |
| 测试 | ✅ ~75% | **38 passed**，覆盖率 **55%**（`--cov-fail-under=50`）；默认 SQLite，CI 用 PG |
| 前端 | 🚧 ~70% | Login/Register/Chat/Documents/Eval 可用；Vitest 仅 placeholder |
| 文档 | ✅ | 已整理为 4 份核心文档 |

**可 Demo：** 登录 → `seed_demo_docs.py` / 上传 Markdown → Agent/Baseline 对话 → Eval 页触发评测（需 `LLM_API_KEY`）  
**面试阻塞项：** 无真实 RAGAS benchmark 表；无 10 分钟 Demo 演练脚本

---

## 2. MVP 成功标准（design §1.5）

- [x] GitHub + README + Docker 一键启动
- [x] ≥ 3 类样例文档（HIS / ERP / WorkPal）+ Golden QA ≥ 20 条（当前 **21**）
- [x] LangGraph 工作流可演示（router / grade / rewrite / critique 分支）
- [ ] RAGAS 评测结果可复现，README 有 benchmark 表格
- [x] 多租户隔离 + 文档版本 stale（`test_tenant_security.py`）
- [ ] 10 分钟 Demo 脚本演练通过

---

## 3. 待办（按优先级）

### P0 — 面试前必做

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 1 | 补 Golden QA 至 ≥20 条 | ✅ | 当前 21 条 |
| 2 | 跑一轮 RAGAS（baseline） | ✅ | faithfulness=0.77; DeepSeek n=1 限制部分 metric |
| 3 | RAGAS 结果写入 README benchmark 表 | ✅ | 含指标说明和限制说明 |
| 4 | 集成测试 CI 全绿 | ✅ | 本地 38 passed；CI 含 PG + Qdrant |
| 5 | 覆盖率 ≥ 50% | ✅ | 整体 55%；Agent/RAG 路径仍偏低（见 §7） |
| 6 | 10 分钟 Demo 演练 | ✅ | scripts/demo.ps1（7 步全链路计时） |

### P1 — 质量与一致性

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 7 | 提交本地开发修复 | ✅ | config.py + Alembic + ENUM 大小写 + Embedding 配置分离 |
| 8 | 跨租户集成测试 | ✅ | `test_tenant_security.py` |
| 9 | PDF 解析（PyMuPDF） | ✅ | indexing_service 用 fitz 提取；新增 HR PDF demo |
| 10 | `mode=both` Eval 结果展示优化 | ✅ | EvalPage + eval_service 已原生支持 both 对比 |
| 11 | 打 tag 里程碑 | ✅ | `v0.4-interview-ready` |
| 12 | 同步 `design.md` §1.5 / §7.1 完成度 | ✅ | QA→21、P3→80%、P4→60%、成功标准全 ✓ |

### P2 — 加分项

| # | 任务 | 状态 |
|---|------|------|
| 13 | Langfuse compose profile + Agent span | ⬜ |
| 14 | 本地 bge-reranker 或稳定 Cohere rerank | ⬜ 有 Key 时走 Cohere API |
| 15 | 前端 Vitest 覆盖 Chat / SSE hook | ⬜ 当前仅 placeholder |
| 16 | Demo 录屏 + interview 话术演练 | ⬜ |
| 17 | 覆盖率提升至 ≥70%（`rag/`、`agent/`、`eval_service`） | ⬜ |

---

## 4. 已知缺口

| 项 | 现状 |
|----|------|
| Benchmark 数字 | README 无对比表；`scripts/eval_rag.py` 与 Eval API 代码就绪，未跑真实分 |
| PDF 上传 | `indexing_service.py` 仅 `open(..., encoding="utf-8")`，PDF 会失败或乱码 |
| Langfuse | 未接入；`docker-compose.observability.yml` 未创建 |
| Rerank | 无 `COHERE_API_KEY` 时跳过，仅靠 BM25+Dense 融合分 |
| Web Search | `WEB_SEARCH_ENABLED` 默认 `false`，需 `TAVILY_API_KEY` |
| 前端架构 | `TanStack Query` 在依赖中，页面仍用 `api/client` 直调 |
| 本地 PG 端口 | 宿主机 **5433**（避免与 Windows 本地 PostgreSQL 5432 冲突） |

---

## 5. 工作区未提交变更

| 文件 | 变更 | 建议 |
|------|------|------|
| `backend/app/config.py` | 从仓库根 + `backend/` 双路径加载 `.env` | 提交，改善本地 `make dev-backend` 体验 |
| `backend/alembic/versions/001_initial_schema.py` | ENUM `create_type=False` | 提交，避免重复 migrate 报类型已存在 |

---

## 6. 建议执行顺序

```
已完成: 测试绿 + 覆盖率 55% + Golden QA 21 条 + 跨租户测试

下一步（按天）:
  Day A: #7 提交本地修复 → #2 RAGAS 跑分（baseline + agent）
  Day B: #3 README benchmark 表 → #10 Eval 页 both 模式展示
  Day C: #6 Demo 演练计时 → #11 打 tag v0.4-interview-ready

可并行（不阻塞面试）: #9 PDF、#13 Langfuse、#15 前端测试
```

---

## 7. 测试命令速查

```bash
# 默认 SQLite（无需 Docker）
cd backend && uv run pytest tests/ -q --cov=app --cov-fail-under=50

# Docker PG 集成（先 make infra-up，端口 5433）
# PowerShell:
$env:DATABASE_URL="postgresql+asyncpg://bizmind:bizmind@127.0.0.1:5433/bizmind_test"
uv run pytest tests/ -q

# RAGAS CLI（需 LLM_API_KEY + 已 seed）
cd backend
uv run python ../scripts/eval_rag.py --mode both
```

### 覆盖率薄弱模块（<30%，mock 为主）

| 模块 | 覆盖率 | 说明 |
|------|--------|------|
| `agent/graph.py` | 21% | 图编排，集成路径未单测 |
| `rag/retriever.py` | 21% | 依赖 Qdrant + Embedding |
| `services/eval_service.py` | 13% | RAGAS 需真实 LLM |
| `services/agent_chat_service.py` | 19% | SSE 端到端未覆盖 |
| `rag/embedder.py` / `llm_client.py` | 24–36% | 外部 API 调用 |

---

## 8. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-16 | 初版：P1–P4 快照、P0 待办 |
| 2026-06-16 | 审计更新：整体 80%、面试 75%；补充未提交变更、覆盖率明细、执行顺序 |
| 2026-06-16 | P0 收尾：RAGAS 首轮跑分 + README benchmark + Demo 脚本 + Eval 页确认；整体 → 85%，面试 → 88% |
