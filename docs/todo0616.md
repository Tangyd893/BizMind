# BizMind — 待办清单（2026-06-16）

> 完成度快照与后续任务。文档已精简为 `design` / `api` / `dev` / 本文。

---

## 1. 完成度总览

| 模块 | 状态 | 说明 |
|------|------|------|
| P1 Baseline RAG | ✅ ~95% | Auth、文档索引、Baseline SSE、Docker |
| P2 Agent | ✅ ~95% | LangGraph、Hybrid、Parent-Child 分块、Agent SSE |
| P3 生产特性 | 🚧 ~70% | 多租户、stale、限流、Embedding 缓存；跨租户测试、PDF 解析缺 |
| P4 评测包装 | 🚧 ~55% | Eval API + Eval 页；RAGAS 真实跑分、Langfuse、README benchmark 缺 |
| 测试 | 🚧 ~50% | `test_auth`、`test_agent_nodes` 已有；覆盖率待过 50%，需本地 PG |
| 文档 | ✅ | 已整理为 4 份核心文档 |

**可 Demo：** 登录 → seed/上传文档 → Agent/Baseline 对话 → Eval 页触发评测（需 LLM Key）  
**面试阻塞项：** 无真实 RAGAS benchmark 表、Golden QA 未满 20 条

---

## 2. MVP 成功标准（design §1.5）

- [x] GitHub + README + Docker 一键启动
- [ ] Golden QA ≥ 20 条（当前 **12**）
- [x] LangGraph 工作流可演示
- [ ] RAGAS 结果写入 README benchmark
- [x] 多租户隔离 + 版本 stale
- [ ] 10 分钟 Demo 脚本演练通过

---

## 3. 待办（按优先级）

### P0 — 面试前必做

| # | 任务 | 产出 |
|---|------|------|
| 1 | 补 Golden QA 8 条 | `data/golden_qa.jsonl` 达 20 条 |
| 2 | 跑一轮 RAGAS（baseline + agent） | `eval_runs` 数据 + README benchmark 表 |
| 3 | 接通 `scripts/eval_rag.py` CLI | 与 `eval_service` 复用逻辑 |
| 4 | 集成测试在 CI 全绿 | `make infra-up` 后 pytest 无 error |
| 5 | 覆盖率 ≥ 50% | `--cov-fail-under=50` |

### P1 — 质量与一致性

| # | 任务 | 说明 |
|---|------|------|
| 6 | 跨租户集成测试 | TC-SEC-01/02 |
| 7 | PDF 解析 | PyMuPDF4LLM 或等价 |
| 8 | `mode=both` eval 结果展示优化 | Eval 页对比 baseline/agent 指标 |
| 9 | 打 tag 里程碑 | `v0.4-interview-ready` |

### P2 — 加分项

| # | 任务 |
|---|------|
| 10 | Langfuse compose profile + Agent span |
| 11 | 本地 bge-reranker 或稳定 Cohere rerank |
| 12 | 前端 Vitest 覆盖 Chat/SSE hook |
| 13 | Demo 录屏 + interview 话术演练 |

---

## 4. 已知缺口

| 项 | 现状 |
|----|------|
| Benchmark 数字 | README 为「待测」，需真实 RAGAS 跑分 |
| Langfuse | 未接入 |
| `docker-compose.observability.yml` | 未创建 |
| `scripts/eval_rag.py` | CLI 仍为 stub |

---

## 5. 建议执行顺序（本周）

```
Day 1: #4 #5 测试与覆盖率
Day 2: #1 #2 #3 评测数据与跑分
Day 3: #7 或 Demo 演练
Day 4: #8 #9 包装与 tag
```

---

## 6. 文档整理记录（2026-06-16）

合并删除：`requirements`、`architecture`、`database-schema`、`agent-workflow`、`p1-backlog`、`progress`、`decisions`、`coding-standards`、`deployment`、`eval-benchmark`、`interview-talking-points`、`test-plan`、`development-guide`、`adr/` 等。

保留：`design.md`、`api.md`、`dev.md`、`todo0616.md`、`README.md`。
