# BizMind — 待办清单（2026-06-19）

> 承接 [todo0616.md](./todo0616.md)（v0.4-interview-ready），**v0.5-quality** 已交付。下一阶段：**v0.6-observability**。  
> 文档体系：`design` / `api` / `dev` / 本文。  
> **最后审计：** 2026-06-19（`main`；tag `v0.5-quality`；pytest **~76** passed；覆盖率 **≥60%**（CI 门槛）；前端 Vitest 含 Chat/SSE）

---

## 1. 完成度总览

| 维度 | 评估 | 较 06-16 变化 |
|------|------|---------------|
| **整体 MVP** | ~**90%** | +5% — 四类 demo 文档、Admin 页、测试加固 |
| **面试就绪** | ~**92%** | +4% — baseline vs agent benchmark 对比表 |
| **生产就绪** | ~**62%** | +7% — Admin UI、集成测试补全；Langfuse 仍缺 |

| 模块 | 状态 | 说明 |
|------|------|------|
| P1 Baseline RAG | ✅ ~95% | Auth、Markdown/PDF 索引、Baseline SSE、Docker |
| P2 Agent | ✅ ~95% | LangGraph 全链路、Hybrid、Parent-Child、Agent SSE |
| P3 生产特性 | ✅ ~88% | 多租户、stale、限流、PDF 解析 + HR demo 入库、Admin 页 |
| P4 评测包装 | 🚧 ~75% | Eval API/页/CLI、README 双模式 benchmark、`data/eval-results.json`；Langfuse 缺 |
| 测试 | ✅ ~82% | **~76** passed，覆盖率 **≥60%**；新增 graph/retriever/eval/agent_chat 单测 |
| 前端 | ✅ ~82% | Login/Register/Chat/Documents/Eval/**Admin**；Vitest Chat + SSE |
| 文档 | ✅ ~95% | 核心文档 + 本文与 `design.md` 已同步 |

**可 Demo：** 登录 → `seed_demo_docs.py`（含 HR PDF）→ Agent/Baseline 对话 → Eval 页 → Admin 用户列表  
**当前阶段目标：** v0.6 — Langfuse 可观测 + 覆盖率向 70% 推进

---

## 2. MVP 成功标准（design §1.5）— 已全部达成 ✅

- [x] GitHub + README + Docker 一键启动
- [x] ≥ 3 类样例文档 + Golden QA ≥ 20 条（**4 类**含 HR PDF，QA **21**）
- [x] LangGraph 工作流可演示（router / grade / rewrite / critique 分支）
- [x] RAGAS 可复现，README 有 **baseline vs agent** 对比表
- [x] 多租户隔离 + 文档版本 stale（`test_tenant_security.py`）
- [x] 10 分钟内可完成完整 Demo（`scripts/demo.ps1`）

---

## 3. v0.5 已交付（2026-06-19）

| # | 任务 | 交付物 |
|---|------|--------|
| 1 | `.gitignore` 允许 demo PDF 入库 | `!data/demo_docs/**/*.pdf` |
| 2 | HR PDF demo | `data/demo_docs/hr/onboarding-guide.pdf` |
| 3 | 文档四类样例对齐 | README、`docs/README.md` |
| 4 | `todo0616` 归档指向 | 顶部 supersede 链接 |
| 5–6 | RAGAS baseline + agent 对比 | README 表 + `data/eval-results.json` |
| 9 | 覆盖率 ≥60% | 新增 4 个 backend 测试文件；CI `cov-fail-under=60` |
| 10 | 前端 Vitest | `ChatPage.test.tsx`、`sse.test.ts`、`App.test.tsx` |
| 13 | Admin 管理页 | `AdminPage.tsx` + `/admin` 路由 |
| 14 | agent_chat SSE 测试 | `test_agent_chat_service.py` |
| 15 | CI 覆盖率门槛 60 | `.github/workflows/ci.yml` |
| 16 | tag `v0.5-quality` | 本次 push 后打 tag |

---

## 4. 待办（v0.6 及以后）

### P1 — 面试展示收尾

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 7 | 用支持 `n>1` 的模型重跑 RAGAS（如 GPT-4o） | ⬜ | 提升 answer_relevancy / context_precision |
| 8 | Demo 录屏（~10 min）+ `docs/interview-script.md` | ⬜ | 展示层收尾 |

### P2 — 生产化（v0.6-observability）

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 11 | Langfuse compose profile + Agent span | ⬜ | design §7.7 |
| 12 | 本地 bge-reranker 或 Cohere 降级策略文档化 | ⬜ | `dev.md` 补充 |
| 17 | 覆盖率提升至 **≥70%** | ⬜ | `embedder` / `llm_client` mock |
| 18 | CI `cov-fail-under` 60 → 70 | ⬜ | 与 design §7.5 对齐后启用 |

### P3 — 加分项

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 19 | TanStack Query 迁移（threads/documents/eval） | ⬜ | 依赖已装 |
| 20 | Web Search 演示（`TAVILY_API_KEY`） | ⬜ | 节点已有，默认关 |
| 21 | RAGAS nightly CI（可选） | ⬜ | 需 API Key secret |
| 22 | Golden QA 扩至 30+ 条 | ⬜ | 含 PDF / 多跳 |

---

## 5. 已知缺口（2026-06-19）

| 项 | 现状 | 关联待办 |
|----|------|----------|
| RAGAS 部分指标偏低 | context_precision=0；DeepSeek `n=1` 限制 | #7 |
| Langfuse | 未接入 | #11 |
| Rerank | 无 `COHERE_API_KEY` 时跳过；无本地 reranker | #12 |
| Web Search | 默认 `false` | #20 |
| TanStack Query | 仅 `useHealth` 使用 | #19 |
| 面试录屏 / 话术 | 未做 | #8 |
| 本地 PG 端口 | 宿主机 **5433** | 文档约定 |

### 最新 RAGAS 快照（21 QA · DeepSeek + BGE-M3）

| 指标 | Baseline | Agent |
|------|:--------:|:-----:|
| Faithfulness | 0.6983 | 0.6908 |
| Answer Relevancy | 0.1578 | 0.1712 |
| Context Precision | 0.0000 | 0.0000 |
| Context Recall | 0.2353 | 0.1905 |

> 完整 JSON：`data/eval-results.json` · 复现：`uv run python ../scripts/eval_rag.py --mode both`

---

## 6. 建议执行顺序（v0.6）

```
Week 1: #11 Langfuse → #17 覆盖率 70% → #18 CI 门槛
Week 2: #7 GPT RAGAS 重跑 → #8 录屏话术 → tag v0.6-observability

可并行: #12 rerank 文档、#19 TanStack Query、#20 Web Search demo
```

---

## 7. 测试与评测命令速查

```bash
# 默认 SQLite
cd backend && uv run pytest tests/ -q --cov=app --cov-fail-under=60

# Docker PG 集成（端口 5433）
$env:DATABASE_URL="postgresql+asyncpg://bizmind:bizmind@127.0.0.1:5433/bizmind_test"
uv run pytest tests/ -q

# 前端
cd frontend && npm run test

# RAGAS CLI
cd backend && uv run python ../scripts/eval_rag.py --mode both

# Demo
.\scripts\demo.ps1
```

### 测试文件一览

| 文件 | 侧重 |
|------|------|
| `test_graph_routes.py` | LangGraph 条件边路由 |
| `test_retriever.py` | hybrid_retrieve mock |
| `test_eval_service.py` | 数据集加载、RAGAS mock |
| `test_agent_chat_service.py` | SSE 流 mock LLM |
| `ChatPage.test.tsx` / `sse.test.ts` | 前端 Chat + SSE 解析 |

---

## 8. 里程碑路线图

| Tag | 目标 | 状态 |
|-----|------|------|
| `v0.4-interview-ready` | MVP + Demo + baseline benchmark | ✅ |
| `v0.5-quality` | 双模式 benchmark + 测试 + PDF + Admin | ✅ |
| `v0.6-observability` | Langfuse + 覆盖率 ≥70% | ⬜ 当前目标 |

---

## 9. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-16 | [todo0616.md](./todo0616.md)：P0/P1 收尾 |
| 2026-06-19 AM | 初版 todo0619：v0.5 规划 |
| 2026-06-19 PM | v0.5 交付审计：~76 tests、≥60% cov、benchmark 对比、Admin 页 |
