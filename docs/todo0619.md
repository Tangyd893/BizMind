# BizMind — 待办清单（2026-06-19）

> 承接 [todo0616.md](./todo0616.md)（v0.4-interview-ready），**v0.5-quality** 已交付；**v0.6** 进行中。  
> 文档体系：`design` / `api` / `dev` / `interview-script` / 本文。  
> **最后审计：** 2026-06-19（pytest **~88** passed；CI 覆盖率门槛 **70%**；默认栈 DeepSeek + BGE-M3，无 GPT）

---

## 1. 完成度总览

| 维度 | 评估 | 说明 |
|------|------|------|
| **整体 MVP** | ~**92%** | 四类 demo、Admin、双模式 benchmark、面试话术 |
| **面试就绪** | ~**95%** | Demo 脚本 + `interview-script.md` + 架构叙事完整 |
| **生产就绪** | ~**68%** | Langfuse 脚手架已有，节点未全量接入 |

| 模块 | 状态 | 说明 |
|------|------|------|
| P1 Baseline RAG | ✅ ~95% | Auth、Markdown/PDF 索引、Baseline SSE、Docker |
| P2 Agent | ✅ ~95% | LangGraph 全链路、Hybrid、Parent-Child、Agent SSE |
| P3 生产特性 | ✅ ~88% | 多租户、stale、限流、PDF、Admin 页 |
| P4 评测包装 | 🚧 ~80% | 双模式 RAGAS、Eval 全链路、面试话术；Langfuse 接入中 |
| 测试 | ✅ ~85% | **~88** tests，CI **≥70%**；含 `test_llm_embedder` |
| 前端 | ✅ ~82% | 全页面 + Vitest Chat/SSE |
| 文档 | ✅ ~95% | 含 `interview-script.md`、默认栈说明 |

**可 Demo：** `demo.ps1` → 登录 → Agent/Baseline 对话 → Eval → Admin  
**当前目标：** Langfuse 节点接入 + 录屏

---

## 2. MVP 成功标准 — 已全部达成 ✅

- [x] GitHub + README + Docker 一键启动
- [x] ≥ 3 类样例文档 + Golden QA ≥ 20（**4 类 / 21 条**）
- [x] LangGraph 工作流可演示
- [x] RAGAS baseline vs agent 对比表
- [x] 多租户隔离 + 文档版本 stale
- [x] 10 分钟 Demo（`scripts/demo.ps1`）

---

## 3. v0.5 已交付

| 项 | 交付物 |
|----|--------|
| Benchmark 对比 | README 表 + `data/eval-results.json` |
| 测试加固 | graph / retriever / eval / agent_chat / llm_embedder |
| Admin 页 | `/admin` + `AdminPage.tsx` |
| HR PDF demo | `data/demo_docs/hr/onboarding-guide.pdf` |
| 无 GPT 文档 | README / design / dev 默认 DeepSeek + BGE-M3 |
| tag | `v0.5-quality` |

---

## 4. v0.6 进展（本次）

| # | 任务 | 状态 | 交付物 |
|---|------|------|--------|
| 7 | 面试话术（RAGAS 局限说明） | ✅ | `docs/interview-script.md` |
| 11 | Langfuse 脚手架 | 🚧 ~60% | `docker-compose.observability.yml`、`app/observability/` |
| 12 | Rerank 降级策略文档 | ✅ | `dev.md` 故障排查 + design §7.6 |
| 17 | llm/embedder 测试 | ✅ | `test_llm_embedder.py` |
| 18 | CI 覆盖率 70% | ✅ | `.github/workflows/ci.yml` |

---

## 5. 待办（剩余）

### P1 — 展示收尾

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 8 | Demo 录屏（~10 min） | ⬜ | 按 `interview-script.md` 录制 |
| 11b | Agent 节点接入 `trace_agent_node` | ⬜ | 模块已有，graph 未调用 |
| 11c | `langfuse` 加入 CI optional 或文档说明 `uv sync --extra observability` | ⬜ | |

### P2 — 加分项

| # | 任务 | 状态 |
|---|------|------|
| 19 | TanStack Query 迁移 | ⬜ |
| 20 | Web Search 演示 | ⬜ |
| 21 | RAGAS nightly CI | ⬜ |
| 22 | Golden QA 扩至 30+ | ⬜ |
| 23 | tag `v0.6-observability` | ⬜ |

---

## 6. 已知缺口

| 项 | 现状 |
|----|------|
| RAGAS context_* 偏低 | DeepSeek `n=1` 已知局限，**不计划换 GPT** |
| Langfuse | compose + 集成模块已有；节点 trace 未接线 |
| 录屏 | 话术已有，视频未录 |
| Web Search | 默认关闭 |

### RAGAS 快照（21 QA · DeepSeek + BGE-M3）

| 指标 | Baseline | Agent |
|------|:--------:|:-----:|
| Faithfulness | 0.6983 | 0.6908 |
| Answer Relevancy | 0.1578 | 0.1712 |
| Context Precision | 0.0000 | 0.0000 |
| Context Recall | 0.2353 | 0.1905 |

> `data/eval-results.json` · `uv run python ../scripts/eval_rag.py --mode both`

---

## 7. 命令速查

```bash
cd backend && uv run pytest tests/ -q --cov=app --cov-fail-under=70
cd frontend && npm run test
.\scripts\demo.ps1

# Langfuse（可选）
docker compose -f docker-compose.yml -f docker-compose.observability.yml --profile observability up -d
```

---

## 8. 里程碑

| Tag | 状态 |
|-----|------|
| `v0.4-interview-ready` | ✅ |
| `v0.5-quality` | ✅ |
| `v0.6-observability` | 🚧 Langfuse 接线 + 录屏 |

---

## 9. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-19 AM | v0.5 规划 |
| 2026-06-19 PM | v0.5 交付 + push |
| 2026-06-19 | v0.6 进展：interview-script、Langfuse 脚手架、cov 70%、无 GPT 文档 |
