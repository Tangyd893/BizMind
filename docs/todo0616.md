# BizMind — 待办清单（2026-06-16）

> 完成度快照与后续任务。文档已精简为 `design` / `api` / `dev` / 本文。

---

## 1. 完成度总览

| 模块 | 状态 | 说明 |
|------|------|------|
| P1 Baseline RAG | ✅ ~95% | Auth、文档索引、Baseline SSE、Docker |
| P2 Agent | ✅ ~95% | LangGraph、Hybrid、Parent-Child 分块、Agent SSE |
| P3 生产特性 | 🚧 ~80% | 多租户、stale、限流、Embedding 缓存；跨租户测试已补；PDF 解析缺 |
| P4 评测包装 | 🚧 ~55% | Eval API + Eval 页；RAGAS 真实跑分、Langfuse、README benchmark 缺 |
| 测试 | ✅ ~75% | **38 passed**，覆盖率 **~55%**（`--cov-fail-under=50`）；默认 SQLite，CI 用 PG |
| 文档 | ✅ | 已整理为 4 份核心文档 |

**可 Demo：** 登录 → seed/上传文档 → Agent/Baseline 对话 → Eval 页触发评测（需 LLM Key）  
**面试阻塞项：** 无真实 RAGAS benchmark 表

---

## 2. MVP 成功标准（design §1.5）

- [x] GitHub + README + Docker 一键启动
- [x] Golden QA ≥ 20 条（当前 **21**）
- [x] LangGraph 工作流可演示
- [ ] RAGAS 结果写入 README benchmark
- [x] 多租户隔离 + 版本 stale
- [ ] 10 分钟 Demo 脚本演练通过

---

## 3. 待办（按优先级）

### P0 — 面试前必做

| # | 任务 | 状态 |
|---|------|------|
| 1 | 补 Golden QA 8 条 | ✅ 21 条 |
| 2 | 跑一轮 RAGAS（baseline + agent） | ⬜ 需 `LLM_API_KEY` |
| 3 | 接通 `scripts/eval_rag.py` CLI | 🚧 部分完成 |
| 4 | 集成测试在 CI 全绿 | ✅ 本地 38 passed；CI PG |
| 5 | 覆盖率 ≥ 50% | ✅ ~55% |

### P1 — 质量与一致性

| # | 任务 | 状态 |
|---|------|------|
| 6 | 跨租户集成测试 | ✅ `test_tenant_security.py` |
| 7 | PDF 解析 | ⬜ |
| 8 | `mode=both` eval 结果展示优化 | ⬜ |
| 9 | 打 tag 里程碑 | ⬜ `v0.4-interview-ready` |

### P2 — 加分项

| # | 任务 |
|---|------|
| 10 | Langfuse compose profile + Agent span |
| 11 | 本地 bge-reranker 或稳定 Cohere rerank |
| 12 | 前端 Vitest 覆盖 Chat/SSE hook |
| 13 | Demo 录屏 + interview 话术演练 |

---

## 4. 已知缺口

| 项 | 现状 |
|----|------|
| Benchmark 数字 | README 为「待测」，需真实 RAGAS 跑分 |
| Langfuse | 未接入 |
| `docker-compose.observability.yml` | 未创建 |
| 本地 PG 端口 | 宿主机 **5433**（避免与 Windows 本地 PostgreSQL 5432 冲突） |

---

## 5. 建议执行顺序（本周）

```
Day 1: ✅ 测试与覆盖率
Day 2: #2 #3 RAGAS 跑分 + README benchmark
Day 3: Demo 演练 + tag
```

---

## 6. 测试命令速查

```bash
# 默认 SQLite（无需 Docker）
cd backend && uv run pytest tests/ -q --cov=app --cov-fail-under=50

# Docker PG 集成（先 make infra-up，端口 5433）
DATABASE_URL=postgresql+asyncpg://bizmind:bizmind@127.0.0.1:5433/bizmind_test \
  uv run pytest tests/ -q
```
